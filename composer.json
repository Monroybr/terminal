{
    "name": "terminal/tiquetes",
    "description": "API Terminal — tiquetes y correo",
    "require": {
        "php": ">=8.1",
        "phpmailer/phpmailer": "^6.9",
        "dompdf/dompdf": "^3.0"
    },
    "config": {
        "optimize-autoloader": true,
        "sort-packages": true
    }
}
